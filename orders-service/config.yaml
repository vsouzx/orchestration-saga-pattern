server:
  port: ":8081"

mysql:
  dsn: "root:root@tcp(localhost:3307)/orders?parseTime=true"

redis:
  addr: "localhost:6379"
  pass: ""
  db: 0

kafka:
  brokers:
    - "localhost:29092"
  create_order_replies_topic: "orders.replies.create-order"
  confirm_order_replies_topic: "orders.replies.confirm-order"
  cancel_order_replies_topic: "orders.replies.cancel-order"
  confirm_order_topic: "orders.commands.confirm-order"
  cancel_order_topic: "orders.commands.cancel-order"

outbox:
  batch_size: 10

otel:
  exporter_endpoint: "localhost:4317"
